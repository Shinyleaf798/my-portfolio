---
title: "示例笔记 (格式参考, 可删除)"
date: 2026-01-01
type: "general"
category: "示例分类"
tags: ["示例标签"]
summary: "这是一篇示例笔记, 仅用于展示 frontmatter 字段格式。接入真实笔记仓库后, 这个文件夹会被覆盖。"
---

## 这是一个二级标题

写笔记时用这个格式。`type` 若为 `project`, 需额外填写 `relatedProject` 字段,
值为对应项目的文件名(不含 `.md`)。

### 三级标题示例

正文支持普通 Markdown 语法、代码块、列表等。
